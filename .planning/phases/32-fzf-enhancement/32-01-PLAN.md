---
phase: 32-fzf-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dot_zsh.d/external-sync.zsh
  - dot_zshenv
autonomous: true

must_haves:
  truths:
    - "fzf interface uses Dracula colour scheme with rounded borders"
    - "Ctrl+T file picker shows bat syntax-highlighted preview with line numbers in a bottom 40% pane"
    - "Alt+C directory picker shows eza tree (2 levels, icons) in a bottom 40% pane"
    - "File previews use bat with Dracula theme and line numbers"
    - "Directory previews use eza tree instead of tree command"
  artifacts:
    - path: "dot_zsh.d/external-sync.zsh"
      provides: "fzf Dracula theme, widget preview commands, compgen functions"
      contains: "--color"
    - path: "dot_zshenv"
      provides: "_fzf_complete_realpath function with eza for directories"
      contains: "eza"
  key_links:
    - from: "dot_zsh.d/external-sync.zsh"
      to: "FZF_DEFAULT_OPTS"
      via: "export"
      pattern: "--color.*fg:#"
    - from: "dot_zsh.d/external-sync.zsh"
      to: "FZF_CTRL_T_OPTS"
      via: "export"
      pattern: "bat.*--theme.*Dracula"
    - from: "dot_zsh.d/external-sync.zsh"
      to: "FZF_ALT_C_OPTS"
      via: "export"
      pattern: "eza.*--tree"
    - from: "dot_zshenv"
      to: "_fzf_complete_realpath"
      via: "function"
      pattern: "eza.*--tree"
---

<objective>
Configure fzf with Dracula colour scheme and rich widget previews.

Purpose: Deliver a visually unified fzf experience with syntax-highlighted file previews (Ctrl+T) and tree directory previews (Alt+C), all using the Dracula palette.
Output: Updated `external-sync.zsh` with Dracula theming and preview commands, updated `dot_zshenv` with eza-based directory previews.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dot_zsh.d/external-sync.zsh
@dot_zshenv
@dot_zsh.d/variables.zsh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dracula theme and widget preview commands to fzf configuration</name>
  <files>dot_zsh.d/external-sync.zsh</files>
  <action>
Update `external-sync.zsh` with these changes:

1. **FZF_DEFAULT_OPTS** -- Replace the existing block with Dracula colour scheme and layout settings:
   - Add `--color` flag with Dracula palette values. Use the standard Dracula fzf colour values:
     `fg:#f8f8f2,bg:#282a36,hl:#bd93f9,fg+:#f8f8f2,bg+:#44475a,hl+:#bd93f9,info:#ffb86c,prompt:#50fa7b,pointer:#ff79c6,marker:#ff79c6,spinner:#ffb86c,header:#6272a4`
   - Add `--border=rounded` for rounded corner borders
   - Add `--preview-window='bottom,40%,border-rounded'` as default preview window position
   - Keep existing binds (`ctrl-a:toggle`, `ctrl-h:change-preview-window(hidden|)`), `--cycle`, `-i`, `--layout=reverse-list`

2. **FZF_CTRL_T_OPTS** -- Replace preview command:
   ```
   --preview 'bat --color=always --theme=Dracula --number --line-range :200 {}'
   ```
   Note: bat will gracefully handle directories by showing an error, but Ctrl+T is for files (fd default includes files). The `_fzf_complete_realpath` fallback handles mixed content.

   Actually, use the existing `_fzf_complete_realpath {}` but ensure it works well. Since `_fzf_complete_realpath` already dispatches to bat for files and tree for directories, keep using it but the function will be updated in Task 2 to use eza. So keep:
   ```
   --preview '_fzf_complete_realpath {}'
   ```

3. **FZF_ALT_C_OPTS** -- Replace preview command to use eza tree directly (since Alt+C only lists directories):
   ```
   --preview 'eza --tree --level=2 --icons --color=always {}'
   ```

Do NOT change FZF_DEFAULT_COMMAND, FZF_CTRL_T_COMMAND, FZF_ALT_C_COMMAND, FZF_GIT_* exports, or the `_fzf_compgen_*` functions. Only modify the three OPTS exports and FZF_DEFAULT_OPTS.
  </action>
  <verify>
Run `zsh -c 'source dot_zsh.d/external-sync.zsh && echo "$FZF_DEFAULT_OPTS" | grep -q "Dracula" || echo "$FZF_DEFAULT_OPTS" | grep -q "bd93f9" && echo "PASS: Dracula colours set" || echo "FAIL"'` from the chezmoi source directory.

Also verify no syntax errors: `zsh -n dot_zsh.d/external-sync.zsh` should exit 0.
  </verify>
  <done>
FZF_DEFAULT_OPTS includes Dracula colour palette with `--border=rounded` and `--preview-window='bottom,40%,border-rounded'`. FZF_CTRL_T_OPTS uses `_fzf_complete_realpath` for file previews. FZF_ALT_C_OPTS uses `eza --tree --level=2 --icons` for directory previews. All existing non-OPTS configuration preserved unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update _fzf_complete_realpath to use eza for directory previews</name>
  <files>dot_zshenv</files>
  <action>
Update the `_fzf_complete_realpath` function in `dot_zshenv`:

1. **Directory preview** -- Replace the `tree` command with `eza`:
   - Old: `tree -a -I '.DS_Store|.localized' -C "$realpath" | head -100`
   - New: `eza --tree --level=2 --icons --color=always -a --ignore-glob='.DS_Store|.localized' "$realpath" | head -100`
   - This gives 2-level deep tree with nerd font icons per user decision.

2. **File preview** -- Update the bat call in the non-binary file branch to always include line numbers:
   - Old: `bat --number --color=always --line-range :100 --theme="$BAT_THEME" "$realpath"`
   - Keep as-is. `--number` already shows line numbers, `$BAT_THEME` is already set to Dracula in `variables.zsh`. No change needed here.

3. **stdin preview** -- The stdin branch already uses bat with `$BAT_THEME`. No change needed.

Only change the directory preview line. Do NOT modify the startup monitoring code, PATH setup, or any other content in `dot_zshenv`.
  </action>
  <verify>
Run `zsh -n dot_zshenv` to verify no syntax errors (exit 0).

Verify eza is referenced: `grep -q 'eza.*--tree' dot_zshenv && echo "PASS: eza tree in realpath function" || echo "FAIL"`.
  </verify>
  <done>
`_fzf_complete_realpath` uses `eza --tree --level=2 --icons --color=always` for directory previews instead of `tree`. File previews unchanged (bat with line numbers and Dracula theme already configured).
  </done>
</task>

</tasks>

<verification>
1. `zsh -n dot_zsh.d/external-sync.zsh` exits 0 (no syntax errors)
2. `zsh -n dot_zshenv` exits 0 (no syntax errors)
3. FZF_DEFAULT_OPTS contains Dracula colour values (grep for `bd93f9` or `282a36`)
4. FZF_DEFAULT_OPTS contains `--border=rounded` and `--preview-window` with `bottom,40%`
5. FZF_ALT_C_OPTS contains `eza --tree`
6. `_fzf_complete_realpath` function contains `eza --tree`
</verification>

<success_criteria>
- fzf uses Dracula colour palette with rounded borders
- Preview pane positioned at bottom, 40% height, with rounded border
- Ctrl+T shows bat-highlighted file preview via _fzf_complete_realpath
- Alt+C shows eza tree with icons for directory preview
- _fzf_complete_realpath uses eza for directories, bat for files (with line numbers)
- No shell syntax errors in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/32-fzf-enhancement/32-01-SUMMARY.md`
</output>
