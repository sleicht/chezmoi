#!/bin/sh
# Shared dispatcher: repository checkouts must never rewrite global hooks.

repo_hooks="$(git rev-parse --git-common-dir)/hooks"
if [ -x "$repo_hooks/post-checkout" ] && [ ! "$repo_hooks/post-checkout" -ef "$0" ]; then
    exec "$repo_hooks/post-checkout" "$@"
fi

exit 0
